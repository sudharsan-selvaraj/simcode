{
  "name": "emulator-manager",
  "displayName": "emulator-manager",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.76.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "emulatormanager": [
        {
          "id": "emulatormanager.android",
          "name": "Android"
        },
        {
          "id": "emulatormanager.ios",
          "name": "Ios",
          "when": "isMac == true"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "emulatormanager.android",
        "contents": "[Open Settings](command:emulatormanager.openSettings)",
        "when": "emulatormanager.androidHomeNotAvailable == true"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "emulatormanager",
          "title": "Emulator Manager",
          "icon": "media/icon.svg"
        }
      ]
    },
    "configuration": {
      "type": "object",
      "title": "Emulator Manager",
      "properties": {
        "emulatormanager.androidhome": {
          "type": "string",
          "default": "",
          "description": "ANDROID_HOME path"
        }
      }
    },
    "commands": [
      {
        "command": "emulatormanager.openSettings",
        "title": "Open emulator manager setting",
        "category": "Emulator Manager"
      },
      {
        "command": "emulatormanager.android.Refresh",
        "title": "Refresh",
        "icon": "$(extensions-refresh)"
      },
      {
        "command": "emulatormanager.ios.Refresh",
        "title": "Refresh",
        "icon": "$(extensions-refresh)"
      },
      {
        "command": "emulatormanager.android.Start",
        "title": "Start",
        "icon": "$(debug-start)"
      },
      {
        "command": "emulatormanager.android.Stop",
        "title": "Stop",
        "icon": "$(stop-circle)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "emulatormanager.android.Refresh",
          "when": "view == emulatormanager.android",
          "group": "navigation"
        },
        {
          "command": "emulatormanager.ios.Refresh",
          "when": "view == emulatormanager.ios",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "emulatormanager.android.Start",
          "when": "view == emulatormanager.android && viewItem == stopped",
          "group": "inline"
        },
        {
          "command": "emulatormanager.android.Stop",
          "group": "inline",
          "when": "view == emulatormanager.android && viewItem == running"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/lodash": "^4.14.191",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.76.0",
    "@typescript-eslint/eslint-plugin": "^5.53.0",
    "@typescript-eslint/parser": "^5.53.0",
    "@vscode/test-electron": "^2.2.3",
    "eslint": "^8.34.0",
    "glob": "^8.1.0",
    "mocha": "^10.2.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "execa": "^7.1.1",
    "lodash": "^4.17.21",
    "node-simctl": "^7.1.12"
  }
}
